<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyJanDergyMusic" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/logo_blue</item>
        <item name="android:windowBackground">@color/surface_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.MyJanDergyMusic" parent="Base.Theme.MyJanDergyMusic" />

    <style name="RoundedImageView" parent="">
        <item name="cornerSize">12dp</item>
    </style>

    <style name="RoundedImageViewLarge" parent="">
        <item name="cornerSize">28dp</item>
    </style>
</resources>
